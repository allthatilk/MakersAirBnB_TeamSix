<h1>Current listings</h1>

<form action="/">
  <input type="date" name="date"></input>
  <input type="submit" value="See Available Spaces"></input>
</form>

<ul>
  <% @spaces.each do |space| %>
    <li>
      <a href="/space?id=<%= space.id %>"><%= space.name %></a>
      <%= space.description %>
      <%= space.price %>
      <img src="<%= space.url %>">
    </li>
  <% end %>
</ul>
